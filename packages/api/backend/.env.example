# Module 6: API & Integrations - Environment Variables

# Server
NODE_ENV=development
PORT=3005

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/qr_platform

# Redis
REDIS_URL=redis://localhost:6379

# Test User (for standalone mode)
# In production, remove this and use JWT from Module 1
TEST_USER_ID=00000000-0000-0000-0000-000000000000

# API Configuration
API_KEY_ENVIRONMENT=test  # 'live' in production

# Rate Limiting
GLOBAL_RATE_LIMIT=1000     # Requests per hour per IP
API_KEY_RATE_LIMIT=100     # Default requests per hour per API key

# Webhooks
WEBHOOK_TIMEOUT_MS=5000    # Default webhook timeout
WEBHOOK_MAX_RETRIES=3      # Default retry attempts

# CORS
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=info
